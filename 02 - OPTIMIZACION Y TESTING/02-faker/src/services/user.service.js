import { UserModel } from '../models/user.model.js';
import {generateUser} from '../utils/user.utils.js';

export const createUsersMock = async () => {
  try {
    const 
  } catch (error) {
    throw new Error(error);
  }
};

export const getUsers = async() => {
  try {
    
  } catch (error) {
    throw new Error(error);
  }
};

